services:
  assistme-gateway:
    image: ${ASSISTME_IMAGE:-assistme:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      ASSISTME_GATEWAY_TOKEN: ${ASSISTME_GATEWAY_TOKEN}
      ASSISTME_ALLOW_INSECURE_PRIVATE_WS: ${ASSISTME_ALLOW_INSECURE_PRIVATE_WS:-}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${ASSISTME_CONFIG_DIR}:/home/node/.assistme
      - ${ASSISTME_WORKSPACE_DIR}:/home/node/.assistme/workspace
      ## Uncomment the lines below to enable sandbox isolation
      ## (agents.defaults.sandbox). Requires Docker CLI in the image
      ## (build with --build-arg ASSISTME_INSTALL_DOCKER_CLI=1) or use
      ## docker-setup.sh with ASSISTME_SANDBOX=1 for automated setup.
      ## Set DOCKER_GID to the host's docker group GID (run: stat -c '%g' /var/run/docker.sock).
      # - /var/run/docker.sock:/var/run/docker.sock
    # group_add:
    #   - "${DOCKER_GID:-999}"
    ports:
      - "${ASSISTME_GATEWAY_PORT:-18789}:18789"
      - "${ASSISTME_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${ASSISTME_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  assistme-cli:
    image: ${ASSISTME_IMAGE:-assistme:local}
    network_mode: "service:assistme-gateway"
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      HOME: /home/node
      TERM: xterm-256color
      ASSISTME_GATEWAY_TOKEN: ${ASSISTME_GATEWAY_TOKEN}
      ASSISTME_ALLOW_INSECURE_PRIVATE_WS: ${ASSISTME_ALLOW_INSECURE_PRIVATE_WS:-}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${ASSISTME_CONFIG_DIR}:/home/node/.assistme
      - ${ASSISTME_WORKSPACE_DIR}:/home/node/.assistme/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - assistme-gateway
