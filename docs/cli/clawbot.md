---
summary: "CLI reference for `assistme clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `assistme clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `assistme clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `assistme clawbot qr` (same behavior as [`assistme qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `assistme clawbot qr` -> `assistme qr`
